/*
Now you have to write a function that takes an argument and returns the square of it.
*/

const square = (n) => n ** 2;
const square2 = (n, p) => Math.pow(n, p);


console.log(square(3)) //> 9
console.log(square2(3, 2)) //> 9
